

在嵌入式开发领域，系统的稳定性往往比功能性更为重要。一个长期运行的系统（如工业控制器、智能家居网关）难免会遇到电磁干扰、电源波动或软件逻辑死锁等异常情况。当程序“跑飞”或陷入死循环时，谁来拯救你的系统？答案就是——**看门狗（Watchdog）**。

今天，我们将深入剖析 STM32 中的**独立看门狗（IWDG）**，揭秘它是如何成为嵌入式系统最后一道安全防线的。

## 看门狗概述

### 为什么需要看门狗？

在由单片机构成的微型计算机系统中，运行环境往往充满挑战。外部的电磁干扰（EMI）可能篡改寄存器或内存数据，导致程序指针（PC）跳转到错误的地址（如非程序区），或者取出错误的指令，最终使程序陷入不可预测的死循环或逻辑混乱。

一旦发生这种情况，如果系统处于无人值守状态（如野外的气象站），后果将是灾难性的——系统停滞，功能失效。

**看门狗（Watchdog）** 的本质是一个硬件定时器电路。它的工作逻辑非常简单粗暴：
> *“你（CPU）必须定期向我报平安（喂狗），否则我就认为你死了，然后把你重启。”*

看门狗的复位信号通常拥有极高的优先级，能够强制复位整个单片机系统，使其从初始状态重新运行，从而实现故障自愈。

### 工作原理与机制

看门狗芯片或内部模块通常与单片机的一个 I/O 引脚或内部总线相连。
1.  **正常运行**：单片机程序在正常的执行流程中，会定期通过软件指令向看门狗发送信号（写入特定寄存器或翻转 I/O 电平），这个动作俗称 **“喂狗”**（Kick the dog）。
2.  **异常发生**：当单片机因干扰跑飞或死锁时，原有的程序逻辑被打断，“喂狗”指令无法被执行。
3.  **超时复位**：看门狗的计数器不断递减（或递增），一旦在规定时间内没有收到“喂狗”信号，计数器溢出，看门狗硬件电路立即产生复位信号，强制重启单片机。

`[此处插入图片：看门狗工作原理示意图，展示正常喂狗与超时复位的流程]`
![](images/新建%20Markdown%20File/image-20260123143752877.png)
## 独立看门狗（IWDG）

STM32 等现代 MCU 通常配备两种看门狗：**独立看门狗（IWDG）** 和 **窗口看门狗（WWDG）**。

**独立看门狗**的“独立”二字体现在其**时钟源**上。它由片内独立的低速时钟（LSI，Low Speed Internal）驱动。

`[此处插入图片：独立看门狗内部框图]`
![](images/新建%20Markdown%20File/image-20260123143832271.png)
### 1. 核心特性
*   **独立时钟**：即使主时钟（HSE/HSI）发生故障停止工作，或者主程序跑飞导致主时钟配置混乱，IWDG 依然能正常工作。这是它作为“最后一道防线”的底气。
*   **硬件隔离**：一旦开启，除非系统复位，否则软件无法通过代码将其关闭（硬件看门狗模式下）。
*   **应用场景**：主要用于对**硬件故障**（如晶振停振）或**严重软件错误**（如死循环）的检测和自动复位。

### 2. 时钟源机制
![](images/新建%20Markdown%20File/image-20260123143832271.png)
IWDG 由内部的 LSI 时钟驱动（通常为 40kHz 或 32kHz，具体视芯片型号而定）。
> [!WARNING] LSI 的精度陷阱
> LSI 是一个 RC 振荡器，受温度和工艺影响较大，精度较低（例如在 STM32F103 中，LSI 频率范围可能在 30kHz~60kHz 之间波动）。因此，IWDG **不适合** 用作高精度的定时任务，只适合做模糊的超时复位保护。我们在计算超时时间时，必须留有足够的**安全余量**。

## 独立看门狗程序设计实战

### 可靠性自检：判断复位源

在系统启动之初，我们非常有必要知道“上一次为什么重启”。如果是看门狗复位，说明系统刚才出故障了，我们可能需要记录日志或报警。

```c
/* 检查当前复位是否由独立看门狗导致 */
if (RCC_GetFlagStatus(RCC_FLAG_IWDGRST) != RESET)
{
    /* IWDGRST flag set */
    // 检测到看门狗复位，可能发生了严重错误
    printf("Warning: System reset by IWDG!\r\n"); 
    // 此处可以添加错误记录逻辑，如写入 Flash 日志
    RCC_ClearFlag(); // 清除标志位，避免误判
}
else
{
    /* IWDGRST flag is not set */
    printf("System normal reset (Power-on or Pin reset).\r\n");
}
```

### 初始化与配置流程

配置 IWDG 需要遵循特定的“解锁”流程，这是为了防止程序跑飞后误操作看门狗寄存器。

```c
void IWDG_Configuration(void)
{
    /* 1. 解除写保护 */
    // 写入 0x5555 到关键字寄存器，允许访问 PR 和 RLR 寄存器
    IWDG_WriteAccessCmd(IWDG_WriteAccess_Enable);

    /* 2. 设置预分频系数 */
    // 假设 LSI = 32kHz (具体看手册)
    // 分频系数 256，则 IWDG 时钟 = 32000 / 256 = 125Hz
    // 周期 T = 1 / 125 = 8ms
    IWDG_SetPrescaler(IWDG_Prescaler_256);

    /* 3. 设置重装载值（超时时间） */
    // 设定超时时间为 1 秒
    // 计数值 = 时间 / 周期 = 1s / 8ms = 125
    // 写入值 = 125 - 1 (计数是从 N 减到 0)
    IWDG_SetReload(125 - 1); 
    
    // 如果需要 2 秒超时：(2000ms / 8ms) - 1 = 249

    /* 4. 首次喂狗 */
    // 将重装载寄存器的值加载到计数器中
    IWDG_ReloadCounter();

    /* 5. 开启看门狗 */
    // 一旦开启，LSI 振荡器将由硬件强制打开
    IWDG_Enable();
}
```

> [!NOTE] 喂狗公式
> $T_{out} = \frac{4 \times 2^{PR} \times (RLR + 1)}{LSI}$
> 其中 $PR$ 是预分频系数，$RLR$ 是重装载值。

### 喂狗策略验证

为了验证看门狗是否有效，我们可以设计以下实验：

**实验 A：正常喂狗**
在 `main` 函数的主循环中，每隔 500ms 喂一次狗（超时时间设为 1000ms）。
```c
while(1)
{
    IWDG_ReloadCounter(); // 喂狗，刷新计数器
    delay_ms(500);        // 延时小于超时时间
}
```
*结果*：系统稳定运行，串口无复位打印。

**实验 B：超时未喂狗**
在主循环中，故意延时超过 1000ms。
```c
while(1)
{
    IWDG_ReloadCounter();
    delay_ms(500);
    delay_ms(600); // 总延时 1100ms > 1000ms，看门狗溢出
}
```
*结果*：串口不断打印 `iwdg reset cpu`，说明系统在不断重启。

![](images/新建%20Markdown%20File/image-20260123143922992.png)

**实验 C：完全不喂狗**
```c
while(1)
{
    // 什么都不做，模拟程序死锁
}
```
*结果*：系统每隔 1 秒重启一次。

![](images/新建%20Markdown%20File/image-20260123143931928.png)
### 喂狗技巧

**a. 裸机环境：定时器中断喂狗**
在没有操作系统的裸机代码中，有些人喜欢把喂狗放在定时器中断里。
*   **优点**：简单，哪怕 `main` 函数卡死，定时器中断可能还在跑。
*   **缺点**：**极度危险！** 如果 `main` 函数死锁了，但定时器中断依然正常工作，看门狗就无法检测到主程序的故障。这违背了看门狗的初衷。
*   **改进**：通常建议在主循环（while(1)）中喂狗，或者设计一种标志位机制：主循环设置标志，定时器检查标志并喂狗，如果主循环卡死不更新标志，定时器就不喂狗。
![](images/新建%20Markdown%20File/image-20260123143954749.png)

**b. RTOS 环境：独立任务喂狗**
在使用 FreeRTOS 或 UCOS 等实时操作系统时，建议创建一个**低优先级的监视任务**（Monitor Task）专门负责喂狗。
*   **逻辑**：该任务监视其他关键任务的运行状态（例如通过事件组或计数器）。只有当所有关键任务都正常“报活”时，监视任务才执行 `IWDG_ReloadCounter()`。
*   **优势**：如果操作系统内核崩溃，或者某个关键高优先级任务死锁（导致低优先级喂狗任务无法调度），看门狗都能准确触发复位。

## 应用领域与现实意义

### 消费电子的“重启大法”
我们常说手机、平板死机了“重启一下就好”。其实在设备内部，如果软件彻底卡死，用户无法操作关机菜单时，长按电源键强制重启的背后，往往就有硬件复位电路在起作用。而设备日常运行中的偶尔自动重启，很多时候就是看门狗在后台默默处理了软件崩溃，用户感知到的只是“屏幕闪了一下”。
![](images/新建%20Markdown%20File/image-20260123144019610.png)

### 工业控制的“无人值守”
在电梯控制、汽车 ECU、远程气象站等场景，系统一旦死机，人工重启成本极高甚至不可能。看门狗是这些系统实现 **7x24 小时高可用性（High Availability）** 的基石。它确保了系统在遇到偶发性故障时，能够自动恢复到已知的安全初始状态。


![](images/新建%20Markdown%20File/image-20260123144042615.png)

---

**总结：**
独立看门狗不是为了修复 bug，而是为了在 bug 发生或外界干扰导致系统失控时，提供一种**挽救机制**。合理设计喂狗策略，是区分“玩具级代码”与“工业级产品”的重要分水岭。