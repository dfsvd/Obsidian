# 搭建Keil MDK 模板
## 准备工作
- **目标芯片**：STM32F407VGT6 (或 ZGT6/IGT6)
- **所需库文件**：STM32F4xx_DSP_StdPeriph_Lib (或从 Keil DFP 包中提取)
## 建立文件夹结构
在电脑上新建一个工程根目录（例如 `Template`），并在其中建立 4 个子文件夹：
- **`USER`**：存放用户代码
- **`CORE`**：存放启动文件、内核头文件、系统时钟文件
- **`FWLIB`**：存放 ST 官方标准外设库的源码 (`src`) 和头文件 (`inc`)
- **`OBJ`**：存放编译过程产生的中间文件 (让工程目录更干净)
    
## 复制必要文件 (核心步骤)
请从库文件包中将文件复制到对应文件夹：
### A. 填充 `FWLIB` (外设驱动)

把库里的 `src` (源文件) 和 `inc` (头文件) 下的所有文件都复制进来

> **⚠️ 关键避坑**：
> - 复制完后，立即删除 **`stm32f4xx_fmc.c`**！
> - **原因**：F407 使用的是 **FSMC**，不支持 **FMC** (F429才用)如果不删，编译必报错
### B. 填充 `CORE` (系统核心)
需要复制以下 3 类文件：
1. **启动文件**：`startup_stm32f40_41xxx.s`
2. **系统文件**：`system_stm32f4xx.c` 和 `system_stm32f4xx.h`，以及 `stm32f4xx.h`
3. **CMSIS 内核文件**：
    - `core_cm4.h`
    - `cmsis_version.h`
    - `cmsis_compiler.h`
    - `cmsis_armcc.h`
    - _(建议把 CMSIS/Include 目录下的所有 .h 都复制进去，以防万一)_
### C. 填充 `USER` (用户文件)
1. 复制 `stm32f4xx_it.c`, `stm32f4xx_it.h`
2. 复制 `stm32f4xx_conf.h`
3. 新建一个空的 `main.c`

---

## 3. Keil 工程配置

### A. 新建工程

1. 打开 Keil -> `Project` -> `New uVision Project`
    
2. 保存路径选在 **`USER`** 文件夹内
    
3. **选择芯片**：`STM32F407VG`
    
4. **弹窗处理**：遇到 "Manage Run-Time Environment" 窗口，**直接点 Cancel 关闭**
    

### B. 添加文件 (Manage Project Items)

点击“品”字形图标，建立组并添加文件：

- **`USER`** 组：添加 `main.c`, `stm32f4xx_it.c`, `stm32f4xx_conf.h`
    
- **`CORE`** 组：添加 `startup_stm32f40_41xxx.s` (类型选 All files), `system_stm32f4xx.c`
    
    - _> 漏加 system_stm32f4xx.c 会报 `Undefined symbol SystemInit` 错误_
        
- **`FWLIB`** 组：添加你需要的外设 `.c` 文件 (如 `stm32f4xx_gpio.c`, `stm32f4xx_rcc.c` 等，或者全加)
    

### C. 魔术棒设置 (Options for Target)

1. **Output 选项卡**：
    
    - 勾选 `Create HEX File`
        
    - `Select Folder for Objects` -> 选择 **`OBJ`** 文件夹
        
2. **C/C++ 选项卡 (重中之重)**：
    
    - **Define (宏定义)**：输入 `STM32F40_41xxx,USE_STDPERIPH_DRIVER`
        
    - **Include Paths (头文件路径)**：添加 `.\USER`, `.\CORE`, `.\FWLIB`
        
3. **Debug 选项卡**：
    
    - 选择你的下载器 (ST-Link / CMSIS-DAP)
        
    - 点击 Settings -> Flash Download -> 勾选 **`Reset and Run`** (下载后自动运行)
        

---

## 4. 代码修正 (解决报错的关键)

### A. 修正 `stm32f4xx_conf.h`

Keil 自动生成的 conf 文件依赖 RTE 环境你需要全选删除该文件内容，替换为**手动版配置**（即手动 `#include` 所有外设头文件）

**通用模板代码：**

```c
#ifndef __STM32F4xx_CONF_H
#define __STM32F4xx_CONF_H

/* Includes ------------------------------------------------------------------*/
/* 开启所有标准外设库头文件 */
#include "stm32f4xx_adc.h"
#include "stm32f4xx_can.h"
#include "stm32f4xx_crc.h"
#include "stm32f4xx_cryp.h"
#include "stm32f4xx_dac.h"
#include "stm32f4xx_dbgmcu.h"
#include "stm32f4xx_dcmi.h"
#include "stm32f4xx_dma.h"
#include "stm32f4xx_dma2d.h"
#include "stm32f4xx_exti.h"
#include "stm32f4xx_flash.h"
#include "stm32f4xx_fmc.h"
#include "stm32f4xx_fsmc.h"
#include "stm32f4xx_gpio.h"
#include "stm32f4xx_hash.h"
#include "stm32f4xx_i2c.h"
#include "stm32f4xx_iwdg.h"
#include "stm32f4xx_ltdc.h"
#include "stm32f4xx_pwr.h"
#include "stm32f4xx_rcc.h"
#include "stm32f4xx_rng.h"
#include "stm32f4xx_rtc.h"
#include "stm32f4xx_sai.h"
#include "stm32f4xx_sdio.h"
#include "stm32f4xx_spi.h"
#include "stm32f4xx_syscfg.h"
#include "stm32f4xx_tim.h"
#include "stm32f4xx_usart.h"
#include "stm32f4xx_wwdg.h"
#include "misc.h"

/* Exported types ------------------------------------------------------------*/
/* Exported constants --------------------------------------------------------*/

/* 如果使用外部时钟源，修改这里，否则保持注释 */
/* #define I2S_EXTERNAL_CLOCK_VAL   12288000 */ 

/* Exported macro ------------------------------------------------------------*/
#ifdef  USE_FULL_ASSERT
  /**
  * @brief  用于检查参数的断言宏
  */
  #define assert_param(expr) ((expr) ? (void)0 : assert_failed((uint8_t *)__FILE__, __LINE__))
  void assert_failed(uint8_t* file, uint32_t line);
#else
  #define assert_param(expr) ((void)0)
#endif /* USE_FULL_ASSERT */

#endif /* __STM32F4xx_CONF_H */

```

## 5. 测试代码 (基于你的板子)

在 `main.c` 中写入以下代码，按 **F7** 编译，0 Error 即可下载
## 常见报错速查表

|报错信息|原因|解决方法|
|---|---|---|
|`cannot open source input file "core_cm4.h"`|缺少 CMSIS 内核文件|从 Keil 安装目录复制全套 CMSIS 头文件到 CORE|
|`cannot open source input file "RTE_Components.h"`|conf 文件用了 RTE 版本|替换 `stm32f4xx_conf.h` 内容为手动版|
|`identifier "FMC_Bank1" is undefined`|包含了 FMC 驱动|从工程组中移除 `stm32f4xx_fmc.c`|
|`Undefined symbol SystemInit`|没添加系统文件|在工程组 CORE 中添加 `system_stm32f4xx.c`|
|`Device not found`|芯片包版本不匹配|魔术棒 -> Device -> 手动重新选中 STM32F407VG|
