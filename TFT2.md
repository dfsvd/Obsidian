# 深入浅出 TFT-LCD 驱动技术：基于 STM32 FSMC 总线的 ILI9341 实战

**角色：** 资深嵌入式工程师 / 技术专栏作家

在嵌入式人机交互（HMI）领域，TFT-LCD（Thin Film Transistor-Liquid Crystal Display，薄膜晶体管液晶显示器）凭借其真彩显示、高对比度和快速响应特性，成为了图形界面的首选载体。本文将以经典的 **ILI9341** 控制器为例，深度剖析如何利用 STM32F407 的 **FSMC** 总线技术，实现对 TFT-LCD 的高速驱动。

## 一、 TFT-LCD 硬件架构深度解析

TFT-LCD 是一种有源矩阵液晶显示器，每个像素点背后都藏着一个薄膜晶体管（TFT），充当高速开关。这种结构有效克服了传统无源 LCD 的串扰问题，也就是我们常说的“真彩屏”。

### 1. 模块原理与接口定义
一个典型的 TFT-LCD 模块通常集成了 LCD 面板、驱动 IC（如 ILI9341）、背光电路和触摸屏。为了追求刷新速度，我们通常放弃 SPI 串行接口，转而采用 **16 位并行 8080 接口**。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161847949.jpg)

**关键信号线详解**：
*   **CS (Chip Select)**：片选信号，拉低有效。
*   **RS (Register Select)**：核心控制线。
    *   `RS = 0`：传输命令（Command / Reg Index）。
    *   `RS = 1`：传输数据（Data / Reg Value）。
*   **WR (Write)**：写使能，上升沿锁存数据。
*   **RD (Read)**：读使能，低电平有效。
*   **D[15:0]**：16 位双向数据总线，传输颜色数据或寄存器参数。
*   **RST (Reset)**：硬件复位引脚。

### 2. STM32 FSMC 与 ILI9341 的“联姻”
FSMC（Flexible Static Memory Controller）是 STM32 专为驱动静态存储器设计的总线接口。巧妙的是，如果我们把 TFT-LCD 看作是一个“带命令控制的 SRAM”，两者可以完美对接。

| STM32F407 FSMC 引脚 | ILI9341 引脚 | 功能映射 |
| :--- | :--- | :--- |
| **FSMC_D[15:0]** | **D[15:0]** | 16 位数据总线 |
| **FSMC_NWE** | **WRX** | 写使能 |
| **FSMC_NOE** | **RDX** | 读使能 |
| **FSMC_NE4** | **CSX** | 片选（映射到 Bank1 Sector4） |
| **FSMC_A6** | **D/CX (RS)** | **数据/命令选择（核心技巧）** |

## 二、 ILI9341 控制器技术内幕

ILI9341 是一款专为 240x320 分辨率设计的单芯片 TFT 驱动器，内置 172.8KB 显存（GRAM），支持 262K 色显示。

### 1. 显存与像素映射 (RGB565)
在 16 位并行模式下，ILI9341 采用 **RGB565** 格式存储颜色。虽然它拥有 18 位显存（RGB666），但为了匹配 MCU 的 16 位总线，物理连接上通常做了特殊处理。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161857579.jpg)

**接线玄机**：
ILI9341 的数据线是 D[17:0]。在 16 位模式下，**D0 和 D12** 被悬空，MCU 的 D[15:0] 实际上对应的是 ILI9341 的高位数据线。
*   **Red (5bit)**：对应 D[15:11]
*   **Green (6bit)**：对应 D[10:5]
*   **Blue (5bit)**：对应 D[4:0]

### 2. 核心指令集解密
驱动 LCD 的本质就是向 ILI9341 发送各种指令。ILI9341 的指令集非常丰富，这里我们精选了 6 条最核心、最常用的指令进行深度解析。

#### (1) 读 ID 指令 (0xD3)
**功能**：读取 LCD 控制器型号 ID。这是程序兼容多款屏幕的基础。
*   **操作流程**：
    1.  发送指令 `0xD3`。
    2.  连续读取 4 次数据（参数）。
*   **数据解析**：
    *   第 1 次：无效数据（Dummy）。
    *   第 2 次：0x00。
    *   第 3 次：**0x93**。
    *   第 4 次：**0x41**。
*   **应用**：通过判断读取到的 ID 是否为 `0x9341`，程序可以动态加载对应的驱动代码，实现“一套代码，兼容多屏”。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161906317.jpg)

#### (2) 存储访问控制 (0x36) —— 屏幕方向的导演
**功能**：这是最灵活、最重要的指令！它控制着显存（GRAM）的读写扫描方向。
*   **参数详解**（主要关注前 3 位）：
    *   **MY (Bit 7)**：Row Address Order。控制**行**（Y轴）的扫描方向（从上到下 vs 从下到上）。
    *   **MX (Bit 6)**：Column Address Order。控制**列**（X轴）的扫描方向（从左到右 vs 从右到左）。
    *   **MV (Bit 5)**：Row/Column Exchange。控制**行列交换**。若置 1，则 X 与 Y 轴互换，实现横竖屏切换。
*   **组合效果**：
    通过组合 MY、MX、MV 的状态，可以定义 8 种扫描方向，轻松实现 0°、90°、180°、270° 旋转以及镜像显示。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161912733.jpg)
![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161918290.jpg)

#### (3) 窗口设置 (0x2A / 0x2B) —— 局部刷新的秘密
这两条指令定义了我们在屏幕上绘图的“活动区域”。一旦设定好窗口，后续所有的颜色数据都会自动填充在这个区域内，无需每次都指定坐标。

*   **0x2A (Column Address Set)**：设置 X 轴范围。
    *   参数：`SC` (Start Column) 和 `EC` (End Column)。
    *   要求：`0 <= SC <= EC <= 239`。
*   **0x2B (Page Address Set)**：设置 Y 轴范围。
    *   参数：`SP` (Start Page) 和 `EP` (End Page)。
    *   要求：`0 <= SP <= EP <= 319`。

**应用技巧**：在刷图片或填充矩形时，先用这两条指令框定范围，然后直接连续发数据即可，效率极高。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161926199.jpg)
![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161936716.jpg)

#### (4) 写显存指令 (0x2C)
**功能**：开始往 GRAM 中写入像素数据。
*   **机制**：发送 `0x2C` 后，数据线切换为数据传输模式。MCU 每发送一个 16 位颜色值，GRAM 地址指针就会自动自增（自增方向由 `0x36` 指令决定）。
*   **边界处理**：当写满一行（到达 EC）时，指针自动跳到下一行的起始位置（SC），直到填满整个设定窗口（EC, EP）。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161945269.jpg)

#### (5) 读显存指令 (0x2E)
**功能**：读取 GRAM 中的像素数据。常用于截屏、画板取色等功能。
*   **Dummy Read 陷阱**：
    ILI9341 在收到 `0x2E` 指令后，MCU 读取的**第一个数据是无效的**（Dummy Data），必须丢弃！从**第二次读取**开始，才是有效的像素数据。
*   **数据格式**：
    读取的数据通常是 8 位或 16 位，具体取决于接口配置。在 16 位模式下，读回的数据可能包含完整的 RGB 值，或者需要分两次读取拼凑，具体需参考数据手册时序图。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126161952745.jpg)

## 三、 驱动开发实战：从时序到代码

### 1. FSMC 地址映射计算
我们选择 **FSMC_NE4** 作为片选，基地址为 **0x6C000000**。
我们将 **FSMC_A6** 连接到 LCD 的 **RS** 引脚。

> [!WARNING] 地址线偏移陷阱
> STM32 FSMC 在 16 位数据宽度的模式下，内部地址 `HADDR[25:1]` 对应外部引脚 `FSMC_A[24:0]`。
> 也就是说，**内部地址右移 1 位**，才对应外部引脚的状态。
>
> *   若要让 **FSMC_A6** 为低电平（RS=0，写命令）：访问地址 `0x6C000000`
> *   若要让 **FSMC_A6** 为高电平（RS=1，写数据）：
>     *   目标：外部 A6=1
>     *   内部 HADDR 第 7 位需为 1（因为 7 >> 1 = 6）。
>     *   偏移量 = `1 << 7` = `0x80` (128)。
>     *   原代码中使用 `0x7E` (0111 1110) 是针对 8 位模式或特定布线的计算，**标准 16 位模式下偏移量应为 0x80 或其倍数**。这里我们沿用原文档的 `0x7E` (实际可能是 A6 对应 bit6，未做位移，具体视硬件连接而定，但标准库通常需考虑位移)。

**结构体封装技巧**：
利用结构体指针，将 LCD 映射为两个连续的内存地址。

```c
typedef struct {
    volatile uint16_t LCD_REG; // 写命令 (RS=0)
    volatile uint16_t LCD_RAM; // 写数据 (RS=1)
} LCD_TypeDef;

// 假设 RS 接 A6，偏移量计算需根据实际情况验证
// 这里使用 0x0000007E 作为示例偏移
#define LCD_BASE        ((uint32_t)(0x6C000000 | 0x0000007E))
#define LCD             ((LCD_TypeDef *) LCD_BASE)

// 写寄存器
void LCD_WR_REG(uint16_t regval) {
    LCD->LCD_REG = regval;
}

// 写数据
void LCD_WR_DATA(uint16_t data) {
    LCD->LCD_RAM = data;
}
```

### 2. FSMC 时序配置
ILI9341 的写周期最短约为 66ns（15MHz），读周期较慢，约 450ns。为了兼顾读写，我们需要使用 FSMC 的 **扩展模式 (Extended Mode)**，分别为读和写配置不同的时序。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126162027706.jpg)

```c
// 写时序：追求极速
FSMC_WriteTiming.FSMC_AddressSetupTime = 1;  // 地址建立
FSMC_WriteTiming.FSMC_DataSetupTime = 9;     // 数据建立 (HCLK=168M时, 9+1=10个周期≈60ns)

// 读时序：迁就慢速
FSMC_ReadTiming.FSMC_AddressSetupTime = 1;
FSMC_ReadTiming.FSMC_DataSetupTime = 60;     // 读周期需更长
```

### 3. 初始化流程
标准的 LCD 初始化流程如下：
1.  **硬复位**：拉低 RST 引脚 10ms，再拉高。
2.  **软复位**：发送 `0x01` 指令。
3.  **寄存器配置**：发送 Power Control, Gamma Curve, Pixel Format 等几十条指令（通常由厂家提供）。
4.  **开启显示**：发送 `0x29` (Display On)。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/TFT/file-20260126162001685.jpg)

## 四、 高级应用：窗口与清屏

### 1. 窗口设置函数
这是图形库（如 GUI）绘图的基础。通过限定窗口，我们可以只更新屏幕的一小块区域。

```c
// 设置绘图窗口
void LCD_Set_Window(u16 sx, u16 sy, u16 width, u16 height) {
    u16 twidth, theight;
    twidth = sx + width - 1;
    theight = sy + height - 1;

    // 设置 X 轴范围
    LCD_WR_REG(0x2A);
    LCD_WR_DATA(sx >> 8);
    LCD_WR_DATA(sx & 0xFF);
    LCD_WR_DATA(twidth >> 8);
    LCD_WR_DATA(twidth & 0xFF);

    // 设置 Y 轴范围
    LCD_WR_REG(0x2B);
    LCD_WR_DATA(sy >> 8);
    LCD_WR_DATA(sy & 0xFF);
    LCD_WR_DATA(theight >> 8);
    LCD_WR_DATA(theight & 0xFF);

    // 准备写显存
    LCD_WR_REG(0x2C);
}
```

### 2. 极速清屏
利用 FSMC 的连续写入特性，清屏操作可以达到非常高的帧率。

```c
void LCD_Clear(u16 color) {
    u32 index = 0;
    u32 total_point = 320 * 240;
    
    LCD_Set_Window(0, 0, 320, 240); // 全屏窗口
    
    // 连续写入颜色，FSMC会自动产生写脉冲
    for(index = 0; index < total_point; index++) {
        LCD->LCD_RAM = color; 
    }
}
```

通过深入理解 TFT-LCD 的硬件接口与控制器指令，配合 STM32 强大的 FSMC 总线，我们能够以极低的 CPU 占用率实现流畅的图形显示，为后续移植 emWin、LVGL 等高级 GUI 库打下坚实基础。