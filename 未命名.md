# 绪论

## 数字电子技术的研究对象与核心范畴

数字电子技术是以数字量为核心研究对象，聚焦数字电路的分析、设计、实现与应用的电子技术分支。数字量与模拟量是电子信号的两大核心类型，其中数字量的取值具有离散性（如 0 和 1 组成的二进制序列），而模拟量则呈现连续变化的特征（如连续的电压、电流信号）。

数字电子技术的研究范畴覆盖数字逻辑电路的基础理论、典型数字逻辑器件（如门电路、触发器、编码器、译码器等）的工作原理与应用、数字系统的整体设计方法，以及数字信号的处理与传输逻辑。从应用维度来看，该技术是计算机、通信设备、嵌入式系统、工业控制等领域的底层核心支撑 —— 例如计算机的 CPU 内部通过数以亿计的数字逻辑单元完成指令运算与数据处理，工业控制系统则依靠数字电路实现对生产环节的精准逻辑控制。

与模拟电子技术相比，数字电子技术具备抗干扰能力强、信号存储与传输稳定、运算精度易保障、系统可编程性高等优势；但同时也存在需要模数 / 数模转换环节（当与模拟系统交互时）、高频应用下功耗与散热问题突出等特点，这也是实际工程设计中需要权衡的核心点。

> [!note] 核心区分：数字量与模拟量
> 
> 数字量的离散性使其可通过二进制编码精准表征，且在传输过程中仅需区分高低电平（对应 0 和 1），即便存在小幅噪声干扰也不易丢失信息；而模拟量的连续性使其对噪声敏感，微小的电压波动都可能导致信号失真，这也是数字技术在现代电子系统中占主导地位的关键原因。

## 1.2 数字电子技术的发展历程

数字电子技术的发展可追溯至二十世纪中叶，其演进历程与半导体技术、集成电路技术的突破深度绑定：

1. 电子管阶段（1940s-1950s）：以电子管为核心器件构建数字电路，典型应用为第一台电子计算机 ENIAC。但该阶段电路体积庞大、功耗高、可靠性差，仅能满足基础的数字运算需求。
2. 晶体管阶段（1950s-1960s）：晶体管替代电子管成为核心器件，大幅降低了电路功耗与体积，提升了可靠性。该阶段数字电路开始向工业控制、通信等领域渗透，但电路仍以分立器件为主，集成度低。
3. 中小规模集成电路阶段（1960s-1970s）：集成电路技术的发展使得数十至数千个器件集成在单一芯片上，数字电路的集成度与稳定性进一步提升，推动了计算器、小型计算机等产品的普及。
4. 大规模 / 超大规模集成电路阶段（1970s 至今）：随着光刻技术的突破，芯片集成度呈现摩尔定律的演进趋势，数亿甚至上百亿个晶体管可集成在单一芯片上。该阶段数字电子技术全面覆盖消费电子、人工智能、航空航天等领域，FPGA、MCU、CPU 等核心器件均基于该技术体系发展。

在发展过程中，数字电子技术的核心逻辑始终围绕 “二进制运算” 与 “逻辑控制” 展开，技术突破主要集中在器件集成度、运算速度、功耗控制三个维度。而摩尔定律的逐渐放缓，也推动了数字电子技术向 “异构计算”“低功耗设计”“存算一体” 等新方向演进。

> [!tip] 发展趋势提示
> 
> 当前数字电子技术正朝着 “智能化”“低功耗”“高集成” 与 “可重构” 方向发展，例如 FPGA 的可重构特性使其能适配不同场景的数字逻辑需求，而存算一体芯片则试图解决传统冯・诺依曼架构中 “存储 - 运算” 分离带来的效率瓶颈。

## 1.3 数字电子技术的应用场景

数字电子技术已成为现代电子系统的基础支撑，其核心应用场景包括：

1. 计算领域：从个人计算机的 CPU、GPU 到服务器的算力芯片，均基于数字逻辑电路实现数据运算与指令执行。高性能计算（HPC）与人工智能芯片（如 GPU、NPU）更是通过优化数字逻辑架构，提升并行运算能力。
2. 通信领域：数字通信系统中的调制解调、编解码、信号处理等环节均依赖数字电路实现，5G/6G 通信设备中的基带芯片、射频数字前端均是数字电子技术的核心应用载体。相比模拟通信，数字通信具备抗干扰能力强、易于加密、便于集成等优势，已成为通信技术的主流方向。
3. 工业控制领域：PLC（可编程逻辑控制器）是数字电子技术在工业控制中的典型应用，通过数字逻辑电路实现对生产流程的逻辑控制、时序控制与数据采集。其高可靠性、可编程性使其适配不同的工业生产场景，从汽车制造到化工生产均有广泛应用。
4. 消费电子领域：智能手机、智能家居、可穿戴设备等产品的核心控制单元（MCU/SoC）均基于数字电子技术设计，实现人机交互、数据处理、设备联动等功能。
5. 航空航天领域：航空航天设备中的导航、测控、数据处理系统，需依靠高可靠性的数字电路保障极端环境下的稳定运行，其数字逻辑设计需满足抗辐射、低功耗、高容错的严苛要求。

不同应用场景对数字电子技术的需求存在差异：工业控制场景侧重可靠性与实时性，计算领域侧重运算速度与并行能力，航空航天场景则侧重抗干扰与容错性。在实际设计中，需根据场景需求选择合适的器件（如工业场景优先选择工业级 MCU，航空航天场景选择抗辐射芯片）与逻辑架构。

## 1.4 本课程的学习目标与方法

### 1.4.1 学习目标

本课程的核心学习目标是建立数字电子技术的完整知识体系，具体包括：

1. 掌握数字电路的基础理论：理解二进制、进制转换、逻辑代数等核心概念，能熟练进行逻辑函数的化简与转换。
2. 掌握典型数字器件的工作原理：深入理解门电路、触发器、编码器、译码器、计数器、寄存器等器件的逻辑功能与应用场景，能分析其时序特性与电气特性。
3. 具备数字电路的分析与设计能力：能基于需求分析数字电路的逻辑功能，设计符合要求的组合逻辑电路与时序逻辑电路，并能通过仿真或实验验证设计有效性。
4. 具备工程应用思维：理解数字电路设计中的功耗、可靠性、成本等工程约束，能在设计中权衡技术指标与实际需求。

### 1.4.2 学习方法

数字电子技术是一门理论与实践高度结合的学科，高效的学习方法需兼顾理论理解与实践验证：

1. 理论体系构建：按 “概念 - 原理 - 应用” 的逻辑梳理知识点，例如学习逻辑门时，先理解基本逻辑运算（与、或、非），再掌握门电路的电气特性，最后结合实际电路分析其应用。需避免孤立记忆知识点，注重逻辑关联。
2. 仿真与实验验证：借助 Multisim、Quartus 等仿真工具，对所学电路进行仿真分析，通过波形观察验证逻辑功能；同时结合硬件实验（如面包板搭建电路、FPGA 开发板验证设计），加深对电路实际工作特性的理解 —— 这能有效避免 “纸上谈兵”，理解实际电路中的延迟、噪声等非理想因素。
3. 工程案例分析：结合实际工程案例（如 PLC 控制电机启停、编码器在键盘扫描中的应用），分析数字电路的设计思路与优化方法，理解 “需求导向” 的设计逻辑。
4. 错题与难点复盘：针对逻辑函数化简、时序电路分析等难点，通过错题复盘梳理易错点，总结解题方法；同时结合行业标准与规范，理解数字电路设计的通用性原则。

> [!warning] 常见学习误区
> 
> 1. 仅记忆公式与定理，忽略逻辑原理的理解，导致无法应对灵活的工程设计问题；
> 2. 轻视实验环节，认为 “理论理解即可”，忽略实际电路中的非理想特性（如门电路的传输延迟、电平阈值）；
> 3. 孤立学习知识点，未建立 “器件 - 电路 - 系统” 的层级认知，难以完成复杂数字系统的设计。

# 笔记 2：信息与编码

## 2.1 信息的数字化表征

信息的数字化是将各类原始信息（如文字、图像、声音、物理量）转换为数字量（二进制序列）的过程，其核心原理是利用二进制的离散特性，通过编码实现信息的精准表征。

从本质上看，信息的数字化包含两个核心步骤：采样与量化（针对连续信息）、编码（将离散信息映射为二进制序列）。例如，模拟音频信号的数字化过程为：首先通过采样将连续的音频波形转换为离散的采样点（采样率决定了采样的密集程度，如 44.1kHz 采样率表示每秒采集 44100 个采样点），再通过量化将采样点的幅值转换为离散的数值（量化位数决定精度，如 16 位量化可表示 65536 个幅值等级），最后通过编码将这些数值转换为二进制序列存储或传输。

对于离散信息（如文字），则直接通过字符编码规则映射为二进制序列。不同类型的信息数字化后均具备 “可存储、可传输、可运算” 的特性，这也是数字系统能统一处理各类信息的基础。

> [!note] 关键参数说明
> 
> 采样率与量化位数是模拟信息数字化的核心参数：采样率需满足奈奎斯特采样定理（采样率≥2 倍信号最高频率），否则会出现混叠失真；量化位数越高，信息还原的精度越高，但同时也会增加数据量。例如，CD 音质的音频采用 44.1kHz 采样率 + 16 位量化，其单声道数据率为 44100×16=705600 bps，双声道则翻倍。

## 2.2 数制基础

数制是表示数值的规则，数字电子技术中核心使用二进制，同时涉及十进制、八进制、十六进制的转换与应用。

### 2.2.1 基本数制的定义

1. 十进制（Decimal）：以 10 为基数，包含 0-9 共 10 个数码，进位规则为 “逢十进一”，位权为 10 的幂次（如十进制数 123=1×10²+2×10¹+3×10⁰）。十进制是人类日常使用的数制，但其在数字电路中难以直接实现（需 10 种不同的电平状态，可靠性低）。
2. 二进制（Binary）：以 2 为基数，包含 0、1 两个数码，进位规则为 “逢二进一”，位权为 2 的幂次（如二进制数 101=1×2²+0×2¹+1×2⁰=5）。二进制是数字电路的核心数制，因为仅需两种电平状态（高 / 低）即可表征，符合数字器件的物理特性，且易于实现逻辑运算。
3. 八进制（Octal）：以 8 为基数，包含 0-7 共 8 个数码，进位规则为 “逢八进一”，位权为 8 的幂次。八进制可视为二进制的 “压缩形式”（1 位八进制对应 3 位二进制），常用于简化二进制数的书写（如二进制数 111001 可表示为八进制数 71）。
4. 十六进制（Hexadecimal）：以 16 为基数，包含 0-9、A-F（对应 10-15）共 16 个数码，进位规则为 “逢十六进一”，位权为 16 的幂次。十六进制是数字系统中最常用的简化数制（1 位十六进制对应 4 位二进制），广泛应用于程序设计、寄存器配置等场景（如二进制数 10111100 可表示为十六进制数 BC）。

### 2.2.2 数制转换

数制转换的核心是基于 “位权展开” 与 “基数取余 / 取整” 的规则，确保转换前后数值等价。

1. 非十进制转十进制：按位权展开求和。
    
    示例：二进制数 1101.101 转十进制
    
    1×2³ + 1×2² + 0×2¹ + 1×2⁰ + 1×2⁻¹ + 0×2⁻² + 1×2⁻³ = 8+4+0+1+0.5+0+0.125 = 13.625
    
    示例：十六进制数 2A.8 转十进制
    
    2×16¹ + 10×16⁰ + 8×16⁻¹ = 32+10+0.5 = 42.5
    
2. 十进制转非十进制：
    
    - 整数部分：“除基数取余，逆序排列”。
        
        示例：十进制数 25 转二进制
        
        25÷2=12 余 1；12÷2=6 余 0；6÷2=3 余 0；3÷2=1 余 1；1÷2=0 余 1 → 逆序得 11001。
    - 小数部分：“乘基数取整，顺序排列”（注意：部分小数无法完全转换，需按精度要求截断）。
        
        示例：十进制数 0.625 转二进制
        
        0.625×2=1.25 取整 1；0.25×2=0.5 取整 0；0.5×2=1.0 取整 1 → 顺序得 0.101。
    
3. 二进制与八进制 / 十六进制转换：
    
    - 二进制转八进制：将二进制数从右至左每 3 位分组（不足补 0），每组转换为 1 位八进制数。
        
        示例：二进制数 101110 → 分组为 010 111 0 → 转换为 2 7 0 → 八进制数 270（注：原数 101110 无前置 0，分组后为 10 1110 → 补 0 为 010 1110 → 2 14？修正：101110 共 6 位，分组为 101 110 → 5 6 → 八进制数 56）。
    - 二进制转十六进制：将二进制数从右至左每 4 位分组（不足补 0），每组转换为 1 位十六进制数。
        
        示例：二进制数 1101011 → 补 0 为 0110 1011 → 6 B → 十六进制数 6B。
    - 八进制 / 十六进制转二进制：反向操作，1 位八进制转 3 位二进制，1 位十六进制转 4 位二进制。
    

> [!caution] 转换易错点
> 
> 1. 十进制转非十进制时，小数部分易忽略 “精度截断” 问题，例如十进制 0.1 转二进制是无限循环小数（0.000110011...），需按实际需求保留位数；
> 2. 二进制与八进制 / 十六进制转换时，分组方向易出错（需从右至左分组），且不足位数时补 0 位置错误（仅在左侧补 0，不改变数值大小）。

## 2.3 码制（编码规则）

码制是将数字、字符、状态等信息映射为二进制序列的规则，核心分为 “数值编码” 与 “字符编码” 两类，其设计需满足 “唯一性”“易识别性”“易运算性” 等原则。

### 2.3.1 数值编码

数值编码针对有符号数与无符号数的二进制表示，核心包括原码、反码、补码三种形式，其中补码是数字电路中实现减法运算的核心编码方式。

1. 无符号数编码：所有二进制位均表示数值，位权为 2 的幂次。例如 8 位无符号数的取值范围为 0（00000000）~255（11111111）。
2. 有符号数编码：
    
    - 原码：最高位为符号位（0 表示正，1 表示负），其余位为数值位。例如 8 位原码中，+5=00000101，-5=10000101。原码的优点是直观易懂，但存在 “+0”（00000000）与 “-0”（10000000）两个零值，且减法运算需单独处理，不利于电路实现。
    - 反码：正数的反码与原码相同；负数的反码为符号位不变，数值位按位取反。例如 8 位反码中，+5=00000101，-5=11111010。反码仍存在双零问题（+0=00000000，-0=11111111），仅作为补码的过渡编码。
    - 补码：正数的补码与原码相同；负数的补码为其反码加 1。例如 8 位补码中，+5=00000101，-5=11111011。补码的核心优势是：① 仅存在一个零值（00000000）；② 减法运算可转换为加法运算（如 a-b = a + (-b) 的补码），大幅简化数字电路的运算逻辑。
    

8 位有符号数的编码取值范围：

- 原码：-127（11111111）~+127（01111111）
- 反码：-127（10000000）~+127（01111111）
- 补码：-128（10000000）~+127（01111111）

> [!important] 补码运算核心逻辑
> 
> 数字电路中加法器是基础运算单元，补码通过 “模运算” 将减法转换为加法：对于 n 位补码，模为 2ⁿ，负数的补码 = 模 - | 负数 |。例如 8 位补码的模为 256，-5 的补码 = 256-5=251（11111011），因此 5-3=5+(-3)=00000101+11111101=100000010，舍弃进位后得 00000010（即 2），实现减法运算。

### 2.3.2 字符编码

字符编码针对非数值信息（如字母、符号、汉字）的二进制表示，核心包括 ASCII 码、BCD 码、Unicode 等。

1. ASCII 码（美国信息交换标准代码）：
    
    基于 7 位二进制编码，可表示 128 个字符，包括大小写字母、数字 0-9、标点符号、控制字符（如换行、回车）。例如：数字 0 的 ASCII 码为 0110000（十进制 48），字母 A 的 ASCII 码为 1000001（十进制 65）。扩展 ASCII 码（8 位）可表示 256 个字符，用于兼容特殊符号，但仅适用于英文场景。
    
2. BCD 码（二进制编码的十进制数）：
    
    将十进制的每一位（0-9）转换为 4 位二进制数，核心分为 8421BCD 码、2421BCD 码、余 3 码等，其中 8421BCD 码是最常用的形式（位权为 8、4、2、1）。
    
    示例：十进制数 58 的 8421BCD 码为 0101 1000；十进制数 9 的 8421BCD 码为 1001。
    
    BCD 码的优势是 “人机交互友好”（直接对应十进制数），广泛应用于数码管显示、计算器等场景；缺点是存在无效编码（如 8421BCD 码中 1010-1111 无对应十进制数），且运算需额外的校正逻辑（如 BCD 码加法后若结果≥10，需加 6 校正）。
    
3. Unicode 编码：
    
    为解决 ASCII 码仅支持英文的问题，Unicode 对全球大部分语言的字符进行统一编码，核心包括 UTF-8、UTF-16、UTF-32 等实现方式。其中 UTF-8 是变长编码（1-4 字节），兼容 ASCII 码（单字节字符与 ASCII 码一致），是当前互联网与软件开发的主流字符编码方式。例如：汉字 “中” 的 UTF-8 编码为 E4B8AD（十六进制），对应二进制为 11100100 10111000 10101101。
    

> [!tip] 编码选择原则
> 
> 1. 数值运算场景优先选择补码（简化加减运算逻辑）；
> 2. 十进制显示 / 人机交互场景优先选择 BCD 码；
> 3. 字符处理场景：英文为主选 ASCII 码，多语言场景选 UTF-8；
> 4. 数字电路设计中，需根据硬件接口（如数码管、串口）选择对应的编码规则，避免编码转换错误导致数据失真。

## 2.4 编码的可靠性设计

数字信息在存储与传输过程中易受噪声、干扰影响导致比特位错误，因此编码的可靠性设计至关重要，核心手段是 “差错检测与纠正编码”（检错码 / 纠错码）。

### 2.4.1 检错码

检错码仅能检测出传输 / 存储中的错误，无法纠正，核心包括奇偶校验码、CRC 码（循环冗余校验码）。

1. 奇偶校验码：
    
    在原编码的基础上增加 1 位校验位，使整个编码的 “1” 的个数为奇数（奇校验）或偶数（偶校验）。
    
    示例：8 位 ASCII 码 “01000001”（A），偶校验时校验位为 0（原码 1 的个数为 2，偶数），编码后为 0 01000001；奇校验时校验位为 1（原码 1 的个数 + 1=3，奇数），编码后为 1 01000001。
    
    奇偶校验码的优势是实现简单、成本低，可检测出奇数个比特错误；但无法检测偶数个比特错误，且无法定位错误位置，适用于错误率较低的场景（如短距离串口通信）。
    
2. CRC 码：
    
    基于多项式运算的检错码，通过将信息位视为多项式系数，与预设的生成多项式进行模 2 除法，将余数作为校验位附加到信息位后。接收端通过相同的生成多项式运算，判断余数是否为 0 来检测错误。
    
    CRC 码可检测出大部分错误（包括偶数个比特错误、突发错误），是存储设备（如硬盘）、通信协议（如以太网、USB）中广泛使用的检错码，其检错能力取决于生成多项式的选择（如 CRC-8、CRC-16、CRC-32）。
    

### 2.4.2 纠错码

纠错码可检测并纠正错误，核心包括汉明码、BCH 码、LDPC 码等，适用于错误率较高且无法重传的场景（如卫星通信、存储芯片）。

1. 汉明码：
    
    通过在编码中插入多个校验位，不仅能检测错误，还能定位错误位置并纠正。汉明码的校验位数量 k 与信息位数量 n 满足 2ᵏ ≥ n + k + 1。
    
    示例：4 位信息位（1011），需 3 位校验位（2³=8 ≥ 4+3+1=8），编码后为 7 位汉明码。接收端通过校验位的组合判断错误位置，可纠正 1 位比特错误，检测 2 位比特错误。
    
    汉明码的实现复杂度适中，是中小数据量场景的主流纠错码；但纠错能力有限（仅能纠正 1 位错误），大数据量场景需选择 BCH、LDPC 等高性能纠错码。

> [!note] 可靠性编码应用场景
> 
> 1. 短距离、低错误率通信（如串口）：优先选择奇偶校验码（实现简单）；
> 2. 存储设备、以太网通信：优先选择 CRC 码（检错能力强）；
> 3. 卫星通信、深空探测：优先选择 LDPC 码 / BCH 码（纠错能力强，适配高干扰场景）；
> 4. 消费电子存储（如 NAND Flash）：结合 ECC（纠错码）与 CRC 码，兼顾检错与纠错能力。