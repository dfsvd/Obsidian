# STM32 FSMC 总线：打破芯片边界的存储扩展技术

**角色：** 资深嵌入式工程师 / 技术专栏作家

在嵌入式系统设计中，MCU 内部的 RAM 和 Flash 往往资源有限。当需要连接大容量外部存储器（如 NOR Flash、SRAM）或驱动高分辨率 TFT-LCD 屏幕时，普通的 GPIO 模拟时序不仅效率低下，还会大量占用 CPU 资源。STM32F407 搭载的 **FSMC（Flexible Static Memory Controller，灵活静态存储器控制器）** 正是为解决这一问题而生。它像一座高速桥梁，将外部设备直接映射到 MCU 的内存地址空间，实现像访问内部变量一样访问外部硬件。

## 一、 概述：FSMC 的核心价值

FSMC 是 STM32 芯片内部的一个硬件外设，它能够根据不同的外部设备类型，自动产生相应的读写时序信号（如片选 CS、写使能 WE、读使能 OE 等）。

### 1. 核心优势
* **硬件生成时序**：无需软件模拟 IO 翻转，速度极快且不占用 CPU 时间。
* **内存映射**：外部设备被映射到 STM32 的地址空间（0x60000000 起），读写外部设备等同于指针操作。
* **灵活性**：支持 Intel 8080 和 Motorola 6800 两种经典总线协议，完美适配各类 LCD 驱动芯片。

### 2. 支持设备类型
FSMC 主要支持静态存储器和类似接口的设备：
* **NOR Flash / PSRAM**（LCD 驱动常用）
* **SRAM / ROM**
* **NAND Flash**（支持 ECC 校验）
* **PC Card**（16 位 PC 卡）

## 二、 硬件架构与存储映射

### 1. FSMC 内部框图
FSMC 内部逻辑将外部设备分为两大类：
* **NOR/PSRAM 控制器**：用于驱动 SRAM、NOR Flash 和 LCD。
* **NAND/PC 卡控制器**：用于驱动 NAND Flash。

这两类设备共用地址线（A0~A25）和数据线（D0~D15），但通过不同的**片选信号（Chip Select, CS）** 来区分当前操作的是哪个设备。

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154741812.jpg)

### 2. 存储区域划分（Memory Map）
STM32 将 FSMC 管理的外部存储器空间划分为 **4 个 Bank**，每个 Bank 容量为 256MB，总计 1GB。我们驱动 LCD 主要关注 **Bank1**。

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154759065.jpg)

**Bank1 的细分**：
Bank1 内部又被细分为 4 个子区域（Sub-Bank），每个子区域 64MB，对应不同的**片选信号（NE1 ~ NE4）**。

| 子区域 | 地址范围 | 对应片选引脚 | 寄存器配置 |
| :--- | :--- | :--- | :--- |
| **Bank1_NORSRAM1** | 0x60000000 ~ 0x63FFFFFF | **FSMC_NE1** (PD7) | BTCR[0,1] |
| **Bank1_NORSRAM2** | 0x64000000 ~ 0x67FFFFFF | **FSMC_NE2** (PG9) | BTCR[2,3] |
| **Bank1_NORSRAM3** | 0x68000000 ~ 0x6BFFFFFF | **FSMC_NE3** (PG10) | BTCR[4,5] |
| **Bank1_NORSRAM4** | 0x6C000000 ~ 0x6FFFFFFF | **FSMC_NE4** (PG12) | BTCR[6,7] |

> [!NOTE] LCD 选型策略
> 大多数开发板习惯将 LCD 的片选引脚连接到 **FSMC_NE4**，因此 LCD 的基地址通常为 **0x6C000000**。

## 三、 为什么 TFT-LCD 可以当做 SRAM 用？

这是 FSMC 应用中最巧妙的设计。

### 1. 信号线对比
* **SRAM 接口**：地址线 (A0~A18)、数据线 (D0~D15)、写 (WE)、读 (OE)、片选 (CS)。
* **8080 LCD 接口**：**RS (寄存器/数据选择)**、数据线 (D0~D15)、写 (WR)、读 (RD)、片选 (CS)、复位 (RST)。

对比发现，除了 SRAM 的**地址线**和 LCD 的 **RS 信号**外，其他信号完全一致。

### 2. RS 信号的巧妙映射
SRAM 通过地址线访问不同的存储单元。而 LCD 只需要区分两个“地址”：
* **写命令 (Reg Index)**：RS = 0
* **写数据 (Reg Value)**：RS = 1

**解决方案**：我们将 LCD 的 **RS 引脚** 连接到 FSMC 的某根 **地址线（如 A6）** 上。
* 当 MCU 访问地址 `Base + 0` 时，A6 为低电平 -> RS=0 -> **写命令**。
* 当 MCU 访问地址 `Base + (1<<6)` 时，A6 为高电平 -> RS=1 -> **写数据**。

> [!WARNING] 地址线偏移问题
> STM32 的 FSMC 在驱动 **16 位宽度** 存储器时，内部地址 HADDR[25:1] 对应外部引脚 FSMC_A[24:0]。
> 也就是说，**内部地址右移一位**才对应外部引脚。
>
> * 若 RS 接在 **FSMC_A6**：
>     * 命令地址偏移：0
>     * 数据地址偏移：`1 << (6 + 1)` = 0x80 (即 128)
>     * 如果 Base = 0x6C000000 (NE4)，则：
>         * CMD 地址：`0x6C000000`
>         * DATA 地址：`0x6C000080`

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154809439.jpg)

## 四、 时序配置与模式选择

FSMC 需要根据外部设备的速度来配置读写时序。对于 LCD，我们通常使用 **模式 A (Mode A)**，因为它支持独立的读写时序配置（Extended Mode）。

### 1. 关键时间参数
在异步模式下，我们需要配置三个核心参数（单位为 HCLK 周期）：
* **ADDSET (地址建立时间)**：地址线发出后，到读/写信号有效之前的时间。
* **DATAST (数据建立时间)**：读/写信号保持有效的时间（决定了读写的脉冲宽度）。
* **ADDHLD (地址保持时间)**：读/写信号失效后，地址线还需要保持的时间（模式 A 中通常未用）。

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154824401.jpg)

### 2. 模式 A 写时序 (Write Timing)
对于 LCD 写操作（画点、发命令），速度越快越好。
* **ADDSET**：通常设为 1 或 2 个 HCLK。
* **DATAST**：根据 LCD 驱动芯片手册（如 ILI9341），写周期通常需 > 60ns。若 HCLK=168MHz (6ns)，则 DATAST 需设为 10~15 左右。

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154837649.jpg)

### 3. 模式 A 读时序 (Read Timing)
LCD 的读操作（读 ID、读显存）通常比写操作慢得多，因此需要单独配置更长的 DATAST，否则读取数据会出错。这正是**扩展模式 (Extended Mode)** 的意义所在：它允许读和写使用两套不同的时序寄存器（BWTR 和 BTR）。

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154829402.jpg)

## 五、 ILI9341 驱动芯片时序分析

以常见的 ILI9341 芯片为例，其 8080 接口写操作时序如下：

![](40%20-%20资源/42%20-%20编程语言/STM32/images/FSMC/file-20260126154842805.jpg)

**信号线详解**：
1.  **CSX (片选)**：低电平有效。在 FSMC 中由 **NE4** 自动控制。
2.  **D/CX (数据/命令)**：低电平传命令，高电平传数据。由 **FSMC_Ax (如 A6)** 控制。
3.  **WRX (写使能)**：上升沿锁存数据。由 **FSMC_NWE** 控制。
4.  **RDX (读使能)**：低电平读取。由 **FSMC_NOE** 控制。
5.  **D[17:0] (数据总线)**：双向传输。由 **FSMC_D[15:0]** 控制（16 位模式下）。

通过合理配置 FSMC 的 `ADDSET` 和 `DATAST`，我们可以精确复现上述时序图，实现高达数千万像素每秒的刷新率，这是软件模拟 IO 无法企及的性能。