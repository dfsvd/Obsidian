

在嵌入式通信领域，SPI 协议凭借其极高的传输效率和简洁的硬件结构，成为了开发者必须攻克的五星级核心知识点 1。无论是高速存储（如 W25Q128 硬盘模块）、无线通信（NRF24L01），还是各种显示与触摸屏，SPI 都扮演着至关重要的角色
对于初学者而言，建议首先夯实基本概念，并能熟练编写基础通信协议程序 3。在进阶阶段，则需要具备移植和修改他人驱动程序的能力，最终达到能够根据模块通信数据包（即由基础协议构成的完整过程）来控制复杂硬件模块的水平

---

## SPI 协议
### 定义与特性

**SPI**（Serial Peripheral Interface，串行外设接口）是一种**高速的、全双工、同步的串行通信总线**
它在芯片引脚上极度节省资源，通常仅需四根信号线即可完成复杂的双向数据交换，这不仅节约了芯片引脚，还极大方便了 PCB 的布局空间优化。正因如此，它被广泛集成在 W25Q128（Flash 存储）、RFID 模块、NRF24L01 以及各种 TFT 显示屏中
### 引脚定义
SPI 通信基于**主从模式（Master-Slave）**，典型的信号线包含以下四根：
- **SCLK (Serial Clock)**：串行时钟线，由主机产生，用于同步数据传输
- **MOSI (Master Output Slave Input)**：主机输出、从机输入，数据从主机流向从机
- **MISO (Master Input Slave Output)**：主机输入、从机输出，数据从从机返回主机
- **SS/CS (Slave Select/Chip Select)**：从机选择或片选信号。**注意：该引脚为高电平时，对应的从机工作无效**
     ![](images/SPI%20协议/file-20260109094932563.jpg)

### 通信方式
#### 单机通信
最基础的连接方式，一个主机直接对接一个从机，四根线一一对应即可
![](images/SPI%20协议/file-20260109095025964.jpg)
#### 多机通信（多从机管理）
在实际项目中，主机常需控制多个模块（如同时连接 SD 卡、触摸屏和 W25Q128）
- **连接逻辑**：时钟线（SCLK）、MOSI 和 MISO 是所有模块共用的，但**片选引脚（SS）必须独立**
- **片选机制**：通信时，主机通过拉低特定的 SS 引脚（如 $SS1=0$）来选中目标模块，此时其他模块的 SS 保持高电平以保持静默
![](images/SPI%20协议/file-20260109095050251.jpg)
- **改进方案（菊花链）**：传统的并行片选会导致引脚占用过多，此时可采用菊花链（Daisy Chain）方式。
	- 在这种模式下，数据像流水一样从一个设备传给下一个不断循环直到数据到达目标设备，但缺点是链路中任何一环出问题，后续设备都会失效，且离主机越远优先级越低
![](images/SPI%20协议/file-20260109095332830.jpg)

### 通信的细节过程与同步特性
SPI 是典型的“以物换物”协议。
- **通信流程**：开始前，主机拉低 **SS 引脚**开启模块；通信结束，SS 恢复高电平，释放总线使用权
![](images/SPI%20协议/file-20260109095534183.jpg)
- **同步与全双工**：同步意味着主机每发送 1 位，从机也必须返回 1 位
    - **写操作**：如果你只想发数据给模块，MOSI 发送 8 位数据，此时 MISO 返回的 8 位数据虽然无意义，但也必须接收（由协议特性决定）
    - **读操作**：如果你想从模块读数据，主机必须在 MOSI 上发送 8 位**任意数据**（作为时钟驱动），从而“换回”从机返回的 8 位有效数据
![](images/SPI%20协议/file-20260109100008309.jpg)
- **传输顺序**：默认通常采用最高有效位优先（MSB）原则，即先传输字节的第 7 位
### 通信速度
SPI 的传输速率极快，通常可达 **10Mbps**。以 GEC-M4 开发板为例，其硬件 SPI 高达 37.5Mbps，但受限于外围设备（如 W25Q128），实际常运行在 10Mbps 左右。10Mbps 约等于 **1.19MB/s** 的传输能力，足以应对大多数嵌入式应用所以其通信速度主要受到其外设速度的影响
### 工作模式
SPI 的采样时机由两个核心参数决定：时钟相位（CPHA）和时钟极性（CPOL）
1. **CPOL (时钟极性)**：决定总线空闲时的电平。0 代表空闲为低，1 代表空闲为高
2. **CPHA (时钟相位)**：决定采样时刻。0 代表在第一个跳变沿采样，1 代表在第二个跳变沿采样
这组合成了**四种标准模式**（00、01、10、11），开发者必须根据从机手册调整主机的配置，以确保数据在正确的波形时刻被捕获

> [!note] 跳变沿
> 指信号电平发生突变的瞬间，它是触发数据传输的关键“发令枪”
> - **上升沿**：信号从低电平（0）向高电平（1）跳变的瞬间
> - **下降沿**：信号从高电平（1）向低电平（0）跳变的瞬间
- 模式 0
![](images/SPI%20协议/file-20260109103419427.jpg)
- 模式 1
![](images/SPI%20协议/file-20260109103425461.jpg)
- 模式 2
![](images/SPI%20协议/file-20260109103432213.jpg)
- 模式 3
![](images/SPI%20协议/file-20260109103438669.jpg)
---
## 二、 模拟 SPI 与硬件 SPI
在开发中，我们可以通过两种手段实现 SPI 通信：
### 模拟 SPI 3
**模拟 SPI**（软件 SPI）是指在不使用单片机片内硬件 SPI 外设的情况下，通过软件编程控制普通的 **GPIO 引脚**，手动输出高低电平来模拟 SPI 的通信时序
- **优势**：极具灵活性，可以分配给单片机上任何闲置的 GPIO 引脚，不受硬件引脚绑定的限制
- **缺点**：由于每一位数据的跳变都需要 CPU 运行代码来完成，通信速度相对较慢，存在一层明显的软件中间层开销
![](images/SPI%20协议/file-20260109141108808.jpg)
#### 模拟 SPI 程序的编写
在编写模拟程序时，核心在于精准控制时钟线（SCLK）的跳变与数据位（MOSI/MISO）的读写节奏。由于 SPI 协议通常遵循 **MSB（最高有效位优先）** 传输原则，我们需要通过一个从 7 到 0 的循环来逐位处理字节数据
##### a、 模式 0 (CPOL=0, CPHA=0)
在模式 0 下，时钟线空闲时保持低电平。其编写逻辑遵循“先准备数据，再触发采样”的原则：
1. **准备数据（写）**：在循环开始后，主机首先根据要发送的字节位设置 **MOSI** 的电平
2. **触发上升沿**：将 **SPI_CLK** 拉高（从 0 跳变到 1），此时模块处于工作状态并开始捕捉 MOSI 上的信号
3. **采样返回（读）**：在时钟高电平期间，为了保证全双工同步，即使我们只打算写数据，也需要读取 **MISO** 引脚的电平并存入接收变量中
4. **恢复空闲**：将 **SPI_CLK** 拉回低电平，为下一位的传输做准备
为了确保引脚电平转换后硬件有足够的响应时间，我们在每次电平跳变后通常会加入微小的延时（如 `delay_us(2)`），这能有效避免因信号反射或电容效应导致的误码
```c
/**
 * @brief  模拟SPI的模式0通信
 * @param  byte: 你要发送的数据(1字节)
 * @retval 返回读取到的数据
 */
uint8_t SPI_MODE0_SendRecvByte(uint8_t byte) 
{
    uint8_t data = 0;
    int8_t i = 0;

    for(i=7; i>=0; i--) // SPI协议是MSB传输的
    {
        // 1、主机->从机(写)
        if(byte & (1<<i))
            SPI_MOSI = 1;
        else
            SPI_MOSI = 0;

        // 设置时钟引脚为高电平（让其处于工作中）
        SPI_CLK = 1;
        delay_us(2); // 让其有时间操作

        // 2、从机->主机(读)
        if(SPI_MISO == 1)
            data = data | (1<<i);

        // 设置时钟引脚为低电平（有电平跳变）
        SPI_CLK = 0;
        delay_us(2); // 让其有时间操作
    }
    return data;
}
```
##### b、模式 3 (CPOL=1, CPHA=1)
与模式 0 不同，模式 3 在空闲时时钟线维持高电平。这意味着它的逻辑操作顺序在电平方向上是相反的：
1. **准备数据**：同样先根据 MSB 原则准备好 **MOSI** 电平。
2. **触发下降沿**：将 **SPI_CLK** 拉低，利用这一跳变沿启动通信，使模块进入工作状态
3. **采样与接收**：在时钟低电平期间读取 **MISO** 的状态
4. **恢复高电平空闲**：循环结束前将 **SPI_CLK** 重新置高
这种模式在控制类似 W25Q128 这种支持多种 SPI 模式的存储模块时非常稳定，因为高电平空闲状态能提供更好的抗噪性
```c
/**
 * @brief  模拟SPI的模式3通信
 * @param  byte: 你要发送的数据(1字节)
 * @retval 返回读取到的数据
 */
uint8_t SPI_MODE3_SendRecvByte(uint8_t byte)
{
    uint8_t data = 0;
    int8_t i = 0;

    for(i=7; i>=0; i--) // SPI协议是MSB传输的
    {
        // 1、主机->从机(写)
        if(byte & (1<<i))
            SPI_MOSI = 1;
        else
            SPI_MOSI = 0;

        // 设置时钟引脚为低电平（让其处于工作中）
        SPI_CLK = 0;
        delay_us(2); // 让其有时间操作

        // 2、从机->主机(读)
        if(SPI_MISO == 1)
            data = data | (1<<i);

        // 设置时钟引脚为高电平（有电平跳变）
        SPI_CLK = 1;
        delay_us(2); // 让其有时间操作
    }
    return data;
}
```

### (2) 硬件 SPI
**硬件 SPI** 直接调用芯片内部集成的 SPI 外设硬件模块
- **优势**：传输速度极快，不占用 CPU 资源，协议由硬件自动解析
- 缺点：引脚固定
- **开发方法**：
    - **方法一**：在开发环境（如 Keil）的 Run-Time Environment 中勾选 SPI 驱动库
    - **方法二**：直接引入 STM32 官方标准外设库文件 `stm32f4xx_spi.c`
- **编写技巧**：建议首先参考库函数自带的**使用说明书（How to use this driver）**，通常位于 `.c` 文件的开头注释中，它会明确指出开启时钟、配置 GPIO 复用模式、初始化结构体参数等关键步骤
![](images/SPI%20协议/file-20260109103756899.jpg)
    

---

### 三、 SPI 应用实战

#### (1) W25Q128 存储模块

W25Q128 是一款 128M-bit 的串行 Flash 存储器 38383838。

+1

- **硬件连接**：在 STM32F407 平台上，通常使用 PB14 作片选（F_CS），PB3/4/5 分别作为 SCK/MISO/MOSI 39。
    
- **内存组织结构**：该芯片由 256 个“块”（Block）组成，每块 64KB；每块又包含 16 个“扇区”（Sector），每个扇区 4KB 40。**重要：对 Flash 进行读写时，必须以扇区为操作单位，不能单独修改某个字节** 41。
    
- **指令编程**：操作模块前需查阅时序图，例如“读取厂商 ID”需要发送指令 `90h` 和地址 `000000h`，随后才能从 MISO 获取到厂商 ID（Winbond 为 `EFh`）和设备 ID（`17h`） 42。
    

#### (2) RFID 模块（MFRC522）

RFID 通过无线射频识别目标。当标签（Tag）进入阅读器发出的电磁场时，产生感应电流获得能量，并发送出自身编码信息 43。

- **移植流程**：
    
    1. **硬件检查**：确认杜邦线无弯折损坏，模块无虚焊或连焊 444444444444444444。

        +2
        
    2. **接线**：先接 GND 以保安全。若使用硬件 SPI，需按指定引脚连接（如 PG7/PG8 处理复位和片选，PB3/4/5 处理总线） 45454545。

        +1
        
    3. **资料筛选**：优先看源代码和演示视频，通过阅读“新手必看”文档避坑，而深奥的数据手册和硬件原理图在初次移植时可暂缓 46464646464646464646464646464646。

        +3
        
    4. **工程适配**：将 `mfrc522.c/h` 导入工程，重点修改引脚定义宏（如 `MFRC522_CS`），并将 F1 系列的旧函数替换为 F4 对应的库函数 47474747。

        +1
        
    5. **调试**：在没有显示屏的情况下，利用串口 `printf` 打印寻卡状态、卡号及卡类型信息进行测试 48。
        

---

### 四、 应用领域与项目实践

SPI 的应用几乎涵盖了现代电子产品的所有角落：

- **无线与网络**：NRF24L01 无线模块、W5500 以太网模块 49494949。

    +1
    
- **人机交互**：TFT 显示屏、墨水屏、电阻/电容触摸屏、音频解码器 50。
    
- **存储**：高性能 SD 卡存储方案 51。
    

[ 课堂课后实验 ]：

为了巩固所学，推荐完成**“简单智能考勤系统”**项目：利用 RFID 模块进行刷卡识别，获取实时时间后，将其作为考勤记录永久存储至 W25Q128 芯片中 52525252。进阶挑战可以尝试将考勤数据通过网络模块发送至远程中转服务器，实现云端管理 53。+2

---

**您是否希望我为您详细解析 W25Q128 扇区擦除与页编程的具体 C 语言代码实现？**