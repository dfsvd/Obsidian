# define
 - 含义
	 - 给……下定义；解释
	 - 阐明；界定；规定
	 - 常可以理解为“**说清楚它是什么**”或者“**划定它的范围**”
- 作用
	- 文本替换
- 用途
	- 定义常量
		- `#define 名字内容`
		- `#define PI 3.14159 `
	- 定义带参数的宏
		- `#define 宏名(参数列表) 替换内容`
		- `#define SQUARE(x) ((x) * (x))`