# RS-485 通信详解：工业现场的总线之王

**角色：** 资深嵌入式工程师 / 技术专栏作家

在工业自动化与智能仪表的演进史中，**RS-485**（又称 TIA-485-A）无疑是里程碑式的存在。它从 RS-232 的点对点通信进化而来，以强大的抗干扰能力、超长的传输距离和灵活的多点拓扑结构，彻底解决了工业设备联网的难题。

本文将深入剖析 RS-485 的核心原理、电气特性、硬件设计及实战应用，助你全面掌握这一工业现场的“总线之王”。

## 一、 协议概述：打破孤岛的联网利器

### 1. 为什么是 RS-485？
早期的智能仪表多采用 **RS-232** 接口，但其只能实现“一对一”的点对点通信，无法满足企业信息化对设备联网的需求。**RS-485** 标准应运而生，它定义了**平衡数字多点系统**中驱动器和接收器的电气特性，使得构建多支路、远距离的本地通信网络成为可能。

### 2. 标准背景
RS-485 标准由电信行业协会（TIA）和电子工业联盟（EIA）联合定义，全称为 **TIA/EIA-485**。
*   **TIA** (Telecommunications Industries Association)：制定通讯配线及架构。
*   **EIA** (Electronic Industries Association)：制定接口电气特性。

### 3. 拓扑结构：总线式网络
RS-485 支持**两线制**和四线制接线。
*   **两线制（主流）**：采用**半双工**通信，所有节点挂载在同一对双绞线上（总线式拓扑）。
*   **节点容量**：标准支持 **32** 个节点，使用高阻抗驱动器可扩展至 **256** 个节点。
*   **主从架构**：通常采用“一主多从”模式，主机（Master）轮询从机（Slave），从机被动响应。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129112749064.jpg)

> [!TIP] 💡 特殊应用：极性自动校正黑科技
>
> **痛点**：在长距离布线或复杂现场中，RS-485 的 A/B 线经常被接反（A接B，B接A），导致通信失败。
>
> **原理**：利用二极管的单向导电性构建**全桥整流电路**。
> 1.  **当 A 接 A，B 接 B 时**：电流通过 D1 和 D4 流动，路径正常。
> 2.  **当 A 接 B，B 接 A 时**：电流方向反转，但会通过 D2 和 D3 流动，自动将信号引导至收发器正确的引脚。
>
> **效果**：无论外部线序如何（A/B 正接或反接），收发器内部始终能接收到正确的极性信号，实现**无极性连接**。这在工程现场极大降低了排查接线错误的成本。
>
> **注意**：这种方案会引入约 1.2V 的二极管压降，可能会缩短最大传输距离，适用于短距离或信号幅度裕量较大的场景。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135520374.jpg)

## 二、 信号电平与电气特性

RS-485 的核心优势在于采用了**差分信号（Differential Signal）**传输。

### 1. 差分信号原理
不同于单端信号（相对于 GND 的电压），RS-485 通过两根线（**A** 和 **B**）之间的**电压差**来表示逻辑状态。
*   **抗干扰原理**：噪声通常以**共模信号**的形式同时耦合到 A/B 两根线上。接收器只检测 $V_A - V_B$ 的差值，共模噪声在相减中被相互抵消。
*   **逻辑定义**：
    *   **逻辑 1 (MARK)**：$V_A - V_B > +200mV$
    *   **逻辑 0 (SPACE)**：$V_A - V_B < -200mV$
    *   **不确定态**：$-200mV \le (V_A - V_B) \le +200mV$（通常需通过上下拉电阻避免此状态）

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135543382.jpg)

### 2. 物理层命名规范
为了避免混淆，工业界常采用以下命名：
*   **A 线**：同相端，也标记为 `D+`、`TX+`、`RX+`。逻辑 1 时电压较高。
*   **B 线**：反相端，也标记为 `D-`、`TX-`、`RX-`。逻辑 0 时电压较高。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135611666.jpg)

> [!WARNING] 接地的重要性
> 虽然差分信号不需要地线作为信号参考，但 RS-485 收发器有**共模电压范围（-7V ~ +12V）**限制。如果收发双方地电位差过大（如雷击、设备漏电），超出共模范围将烧毁接口。因此，**必须连接信号地（GND）**以建立公共参考点。

## 三、 硬件设计：SP3485 芯片实战

以经典的 **SP3485**（或 MAX485）收发器为例，解析硬件电路设计。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135551452.jpg)

### 1. 引脚功能详解
| 引脚 | 符号 | 功能描述 | 典型连接 |
| :--- | :--- | :--- | :--- |
| 1 | **RO** | **接收输出** (Receiver Output) | 接 MCU 的 RXD |
| 2 | **RE#** | **接收使能** (Receiver Enable) | 低电平有效，通常与 DE 短接 |
| 3 | **DE** | **驱动使能** (Driver Enable) | 高电平有效，接 MCU 的 GPIO 控制方向 |
| 4 | **DI** | **驱动输入** (Driver Input) | 接 MCU 的 TXD |
| 5 | **GND** | 地 | 系统地 |
| 6 | **A** | 同相端 | 接总线 A |
| 7 | **B** | 反相端 | 接总线 B |
| 8 | **VCC** | 电源 | 3.3V 或 5V |

### 2. 收发控制逻辑
RS-485 是半双工的，必须分时收发。通常将 `RE#` 和 `DE` 短接，由一个 GPIO 控制：
*   **GPIO = 1**：驱动器工作（DE=1），接收器关闭（RE#=1），处于**发送模式**。
*   **GPIO = 0**：驱动器关闭（DE=0，高阻态），接收器工作（RE#=0），处于**接收模式**。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135602182.jpg)

### 3. 典型原理图设计
*   **终端电阻**：在总线的最首端和最末端各并联一个 **120Ω** 电阻，用于匹配传输线阻抗，消除信号反射（驻波）。
*   **上下拉电阻**：A 接上拉，B 接下拉。确保在总线空闲（所有节点均接收）时，A-B 电压差为正，维持逻辑 1（UART 的停止位/空闲态），防止噪声引入乱码。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135725112.jpg)

## 四、 软件开发：驱动与协议

### 1. 模式切换与数据发送
发送数据前需切换为发送模式，发送完毕后**必须等待最后一个字节传输完成**（检查 `TC` 标志位而非 `TXE`），再切回接收模式。

```c
// 485模式控制引脚：1发送，0接收
#define RS485_TX_EN  PGout(8) 

void RS485_Send_Data(uint8_t *pbuf, uint32_t len) {
    uint32_t i;
    
    RS485_TX_EN = 1;      // 切换为发送模式
    delay_us(5);          // 给收发器一点状态切换时间
    
    for(i = 0; i < len; i++) {
        USART_SendData(USART2, pbuf[i]); 
        // 等待发送数据寄存器空
        while(USART_GetFlagStatus(USART2, USART_FLAG_TXE) == RESET);
    }
    
    // 关键：等待最后一个字节发送完成（TC标志）
    while(USART_GetFlagStatus(USART2, USART_FLAG_TC) == RESET);
    
    RS485_TX_EN = 0;      // 发送完毕，立即切回接收模式
}
```

### 2. 中断接收处理
接收通常在中断中进行，收到数据后存入缓冲区待处理。

```c
void USART2_IRQHandler(void) {
    uint8_t d;
    if(USART_GetITStatus(USART2, USART_IT_RXNE) != RESET) {
        d = USART_ReceiveData(USART2);
        // 处理接收到的数据 d ...
        // 推荐使用环形缓冲区或状态机处理 Modbus 帧
        USART_ClearITPendingBit(USART2, USART_IT_RXNE);
    }
}
```

## 五、 应用领域与调试

### 1. 核心应用场景
RS-485 的身影遍布现代工业的各个角落：
*   **工业自动化**：PLC 与传感器、变频器通信（Modbus RTU）。
*   **智能楼宇**：中央空调、门禁系统、楼宇照明控制。
*   **电力系统**：智能电表抄表（DL/T645）、光伏逆变器监控。
*   **安防监控**：云台控制（Pelco-D 协议）。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135534818.jpg)

### 2. 调试利器：USB 转 485 转换器

在进行 RS-485 开发和调试时，一款可靠的转换工具是必不可少的。以下以常见的工业级 USB 转 485 模块为例介绍。

#### 3.1 核心参数解析
市场上主流的 USB 转 485 转换器通常具备以下特性：
*   **芯片方案**：采用 **CH340** 等成熟 USB 转串口芯片，兼容性好。
*   **供电**：USB 5V 直接供电，部分模块对外提供 5V 电源（带保险丝限流）。
*   **通讯能力**：
    *   **波特率**：最高支持 **921600 bps**，甚至更高。
    *   **传输距离**：理论最大 **1200 米**（在 9600 bps 低速下）。
    *   **协议透明**：作为纯硬件物理层转换设备，**不受上层协议限制**（Modbus, Profibus 等均可透传）。
*   **防护等级**：
    *   **TVS 瞬态抑制**：吸收浪涌电压。
    *   **ESD 静电保护**：防止人体静电损坏芯片。
    *   **短路保护**：AB 线短接或与电源短接不烧毁。
*   **环境适应**：工作温度 -40℃ ~ 80℃。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135649839.jpg)

#### 3.2 接口定义与接线
模块通常引出接线端子，定义如下：
*   **A+ / 485+**：接设备端的 A 线。
*   **B- / 485-**：接设备端的 B 线。
*   **GND**：信号地（建议连接，以抑制共模干扰）
*   **GND**：对外供电池（可给其他设备供电，可选）
*   **+5V**：对外供电输出（可选）。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135656281.jpg)

#### 3.3 硬件连接示意
PC 端插入转换器，设备端（如 STM32 开发板）通过双绞线连接。
*   **A 接 A，B 接 B**（若通信不通，尝试交换 A/B，可能是标号定义不一致）。
*   **共地**：虽然 485 是差分信号，但强烈建议将两端的 GND 相连。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135707242.jpg)

#### 3.4 其他样式工具
除了端子式，还有针对不同场景的工业导轨式、简易优盘式转换器。搜索关键字：**USB 转 485**。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135702669.jpg)

## 四、 STM32 原理图参考

以下是基于 STM32 的 RS-485 接口电路设计实例。

### 1. 电路原理图
该设计采用了 SP3485 芯片，包含 120Ω 终端电阻（R19）、上下拉偏置电阻（R13, R16）以及滤波电容。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135725112.jpg)
![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135730433.jpg)

### 2. 引脚映射表
本例中使用 STM32 的 USART2 进行通信，引脚分配如下：

| 功能信号          | STM32 引脚 | 描述                     |
| :------------ | :------- | :--------------------- |
| **USART2_TX** | **PA2**  | 串口发送，接 SP3485 的 DI     |
| **USART2_RX** | **PA3**  | 串口接收，接 SP3485 的 RO     |
| **RS485_RE**  | **PG8**  | 收发控制，接 SP3485 的 DE/RE# |

## 六、 深度拓展：信号与协议

### 1. 信号类型深度对比

理解信号类型是掌握通信物理层的关键。

#### (1) 单端信号 (Single-Ended)
*   **定义**：仅用一根信号线（相对于地线 GND）传输数据。
*   **特征**：逻辑电平由信号线电压相对于 GND 的高低决定（如 TTL 逻辑 1 > 2V, 逻辑 0 < 0.8V）。
*   **优缺点**：电路简单、成本低；但**抗干扰能力差**（易受地电位漂移影响）、传输距离短。
*   **典型应用**：UART (TTL), GPIO, RS-232。

#### (2) 差分信号 (Differential)
*   **定义**：使用两根信号线（A 和 B）传输一个信号，A 线传输原始信号，B 线传输反相信号。
*   **特征**：逻辑电平由两线电压差决定，与地电位无关。
*   **优缺点**：**抗干扰能力极强**、抑制 EMI、时序定位精确；但布线要求高（需等长、紧耦合）。
*   **典型应用**：RS-485, CAN, USB, Ethernet, LVDS。

#### (3) 共模信号 (Common Mode)
*   **定义**：两根信号线上同时出现的相同方向、相同幅度的噪声信号（通常来自 EMI 或地电位差）。
*   **处理机制**：差分接收器通过减法运算（$V_A - V_B$）自动滤除共模信号。RS-485 收发器通常能承受 **-7V ~ +12V** 的共模电压。

| 特性 | 单端信号 | 差分信号 |
| :--- | :--- | :--- |
| **信号线数量** | 1 根 (+GND) | 2 根 (A/B) |
| **抗干扰能力** | 弱 | 强 |
| **传输距离** | 短 (<15m) | 长 (<1200m) |
| **参考点** | GND | 另一根信号线 |

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135749251.jpg)

### 2. 协议栈：Modbus —— 工业界的通用语言

RS-485 只是物理层（“路”），传输什么数据需要应用层协议（“车”）。**Modbus** 是最常用的工业协议，由 Modicon 公司（现施耐德电气）于 1979 年发布。

*   **架构**：一主多从（Master-Slave）。
*   **物理层**：通常运行在 RS-485 或 RS-232 上。
*   **数据模型**：
    *   **线圈 (Coils)**：位操作，读写（如继电器控制）。
    *   **离散输入 (Discrete Inputs)**：位操作，只读（如开关量输入）。
    *   **保持寄存器 (Holding Registers)**：字操作，读写（如参数设定）。
    *   **输入寄存器 (Input Registers)**：字操作，只读（如传感器读数）。
*   **传输模式**：
    *   **Modbus RTU**：二进制传输，效率高，工业首选。
    *   **Modbus ASCII**：ASCII 码传输，调试方便，效率低。

![](40%20-%20资源/44%20-%20硬件/电路设计/images/RS485/file-20260129135803515.jpg)

RS-485 结合 Modbus，构成了工业物联网（IIoT）最坚实的神经末梢。